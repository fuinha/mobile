import 'package:flutter/material.dart';

class PetParksSlidingPanelHeader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Nearby Pet Parks'),
    );
  }
}
